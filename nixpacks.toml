[phases.setup]
nixPkgs = ["php84", "php84Packages.composer", "nodejs_22", "nodePackages.npm"]

[phases.install]
cmds = [
    "composer install --optimize-autoloader --no-dev --no-interaction",
    "npm install",
    "npm run build"
]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"
